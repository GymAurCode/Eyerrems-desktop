DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/rems
JWT_SECRET_KEY=change_me_super_secret
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
UPLOAD_DIR=uploads
PUBLIC_BASE_URL=http://localhost:8000
# Optional: dedicated key for encrypting stored email passwords.
# Falls back to JWT_SECRET_KEY if not set.
# MAIL_ENCRYPTION_KEY=another_strong_secret_key
